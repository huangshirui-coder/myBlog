<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper">
    <select id="getAllUsername" resultType="String">
        select user_name from sys_user
    </select>

    <select id="getUserWithRoleByUsername" resultType="com.example.demo.entity.User">
        select sys_user.*, sys_role.name role from sys_user
        left join sys_user_role on sys_user.id = sys_user_role.user_id
        left join sys_role on sys_user_role.role_id = sys_role.id
        where user_name = #{username}
    </select>

    <sql id="all">

    </sql>

    <select id="pageList" resultType="com.example.demo.entity.User">
        select * from sys_user
        <where>
            <if test="user.userType != null">
                user_type = #{user.userType}
            </if>
            <if test="user.status != null">
                and status = #{user.status}
            </if>
            <if test="searchKey != null || searchKey != ''">
                and (user_name like concat('%',#{searchKey},'%') or email like concat('%',#{searchKey},'%') or phonenumber like concat('%',#{searchKey},'%'))
            </if>
        </where>
        limit #{pageDomain.pageSize} offset #{pageDomain.offset}
    </select>
</mapper>